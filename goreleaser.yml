brew:
  repo: goreleaser/homebrew-tap
  folder: Formula
  dependencies:
    - git
fpm:
  formats:
    -
      name: deb
      dependencies:
        - git
    -
      name: rpm
      dependencies:
        - git
