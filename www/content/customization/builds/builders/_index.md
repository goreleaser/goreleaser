---
title: "Builders"
weight: 1
---

GoReleaser supports multiple languages and build systems through its builder interfaces.
Each builder accepts a build configuration and emits binaries or libraries into the `dist` directory.
