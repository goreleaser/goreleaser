# This file was generated by GoReleaser. DO NOT EDIT.
EAPI=8

DESCRIPTION="foo"
HOMEPAGE=""
SRC_URI="
  amd64? ( https://dummyhost/download//foo_1.0.0_linux_amd64.tar.gz -> foo_1.0.0_linux_amd64.tar.gz )
"

LICENSE="MIT"
SLOT="0"
KEYWORDS="~amd64"
IUSE=""

S="${WORKDIR}"

src_install() {
  exeinto /opt/bin
  doexe "foo" || die "Failed to install binary"
  dobin "bin1" || die "Failed to install file"
  exeinto /opt/foo
  doexe "exe1" || die "Failed to install file"
  doexe "exe2" || die "Failed to install file"
  exeinto /usr/local/bin
  doexe "exe3" || die "Failed to install file"
  insinto /etc/foo
  doins "ins1" || die "Failed to install file"
  insinto /
  doins "ins2" || die "Failed to install file"
  insinto /etc/foo
  doins "ins3" || die "Failed to install file"
  insinto /usr/lib/systemd/system
  doins "sys1.service" || die "Failed to install file"
  newins "sys2.service" "sys2-rename.service" || die "Failed to install file"
}
